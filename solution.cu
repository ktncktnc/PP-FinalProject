#include "solution.cuh"

PnmImage BaseSolution::run(const PnmImage &inputImage, char **argv) {
    /*
     * Return the Input Image;
     * */
    PnmImage outputImage;
    outputImage = inputImage;
    return outputImage;
}